version: '3.8'
services:
  coordinator:
    build:
      context: ./coordinator
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./coordinator:/app
    environment:
      - PYTHONPATH=/app